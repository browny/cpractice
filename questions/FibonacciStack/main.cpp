
#include <iostream>
#include <stack>
using namespace std;

int main ()
{
	stack<int> mystack;

	mystack.push(0);
	mystack.push(1);

	int n;
	cin >> n;
	for (int i = 0; i < i; ++i) {
		if (i == 0 || i == 1)


	}


	for (int i=0; i<5; ++i) mystack.push(i);

	cout << "Popping out elements...";
	while (!mystack.empty())
	{
		cout << " " << mystack.top();
		mystack.pop();
	}
	cout << endl;

	return 0;
}
